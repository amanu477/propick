## Packages
framer-motion | For smooth page transitions and micro-interactions
recharts | For the analytics dashboard visualization
lucide-react | For beautiful icons (already in base but emphasizing usage)
clsx | For conditional class names
tailwind-merge | For merging tailwind classes

## Notes
- Images will use Unsplash for placeholders where product logos/screenshots would be.
- Affiliate links use standard `<a>` tags with `rel="nofollow noopener sponsored"` and point to `/go/:slug` which backend handles.
- The "Link in Bio" page will have a distinct, mobile-first style different from the main site.
- Color palette: White backgrounds, Blue accents (Trust), Green CTAs (Action), Red (Cons/Warnings), Yellow (Highlights).
- Typography: Inter for UI/Body, Outfit for Headings to give a modern tech feel.
